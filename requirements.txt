# StormTrace runtime dependencies.
# Python 3.10 or newer is required (timezone-aware datetime.UTC usage).
#
# Versions are pinned exactly so a CI run and a laptop run analyse the
# same data with the same code.
duckdb==1.5.5
matplotlib==3.11.1
sgp4==2.27
fastapi==0.141.1
uvicorn==0.52.4
boto3==1.43.82
confluent-kafka==2.15.0
scikit-learn==1.9.0
mlflow==3.15.2
prometheus_client==0.26.0
# numpy and scipy arrive with scikit-learn, but src/train_model.py imports
# both directly, so they are declared rather than relied on transitively.
numpy==2.5.2
scipy==1.18.1


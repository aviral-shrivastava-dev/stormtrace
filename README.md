# StormTrace

**An open, research-grade lakehouse-style pipeline that measures how space weather degrades the reliability of public satellite orbit data.**

StormTrace ingests public orbital elements (CelesTrak) and real-time space
weather (NOAA SWPC), preserves immutable point-in-time snapshots, derives
research features with SQL, and produces an explainable **Orbit Reliability
Index** — a trust signal for public orbit data, explicitly *not* a collision
warning system.

## Research Question

> Do geomagnetic disturbances produce measurable, correlated degradation in
> public orbit estimates across populations of LEO satellites, and can that
> degradation be detected from public data alone?

## Architecture

```text
CelesTrak GP (stations, cubesat, science, iridium-33-debris)
CelesTrak SW (daily Kp/Ap/F10.7)      NOAA SWPC RTSW      SatNOGS network
        |                                     |                 |
        v                                     v                 v
  ingest_celestrak.py                  ingest_noaa.py    ingest_satnogs.py
  ingest_space_weather.py
        |                                     |                 |
        +--------------- immutable Bronze ---------------------+
                        |
                load_history.py          (idempotent, checksummed)
                        |
                   DuckDB history
                        |
                check_quality.py        (gate: errors stop the pipeline)
                        |
     +----------------+----------------+
     |                |                |
summarize_history  build_gold    build_orbit_features
     |                |                |
     +----------------+----------------+
                      |
              analyze_research.py      (orbit change, freshness, ORI, charts)
                      |
        build_propagation_disagreement.py  (SGP4 drift, quality-labelled)
                      |
              validate_ori.py         (point-in-time ORI backtest, Spearman)
                      |
              train_model.py          (optional: GBR model, MLflow tracking)
                      |
        data\gold\*.csv  +  data\reports\*.png / research_summary.md
```

Orchestration: `run_pipeline.py` (atomic lock, policy-aware) or the
registered Windows scheduled task `StormTracePipeline` (hourly, with
per-source rate limiting). A step that times out is logged as a failed step
and stops the run cleanly rather than crashing the orchestrator. Each run
ends with three optional steps, logged as skips when their service is
absent: model training, the MinIO sync, and the Redpanda publish. The
status API exposes `/metrics` for the Prometheus + Grafana stack.

## Quick Start

Requires Python 3.10+ on Windows.

```powershell
pip install -r requirements.txt
python src\run_pipeline.py --dry-run   # see what is due, change nothing
python src\run_pipeline.py             # collect, validate, build, analyze
```

Outputs land in `data\gold\` (CSV tables) and `data\reports\` (charts and
`research_summary.md`).

The build log for how this was written, lesson by lesson, is in
[docs/lessons.md](docs/lessons.md).


## Current Findings (from real collected data)

Every number below is regenerated by the pipeline; none is hand-written.
See `data\reports\research_summary.md` for the full report and its
limitations.

- **Source update frequency ≠ data change frequency**: consecutive snapshots
  can contain byte-identical element sets; measured "zero drag" was correctly
  reported as republished data, not as a physical measurement.
- **Real propagation disagreement measured**: across 1,621 analysis-grade
  element pairs (median span 14.3 h), the median SGP4 disagreement is
  0.24 km, and the RIC decomposition separates drag (along-track dominant)
  from maneuvers (radial-dominant, e.g. the MMS formation).
- **Outliers were corrupting the statistics**: 33 pairs fall outside the
  SGP4 analysis envelope (span > 72 h, or disagreement > 500 km, up to
  5,079 km). They are kept in Gold as evidence but excluded from every
  statistic. Before the exclusion, the reported maximum was 5,079 km and
  the ML model learned mostly from those outliers.
- **The Orbit Reliability Index is directionally validated**: median
  measured error rises monotonically across the high → moderate → reduced →
  low predicted classes (0.09 → 0.16 → 0.38 → 2.25 km), Spearman
  score-vs-error is −0.47, and element age correlates with error at +0.35
  in the designed direction. The drag-like subset (along-track dominant)
  scores −0.55, better than the maneuver-contaminated whole.
- **A learned model beats the hand-crafted index**: a gradient-boosted
  model on point-in-time features predicts disagreement rate with pooled
  out-of-fold Spearman 0.58 (grouped by object), against the index's 0.30
  vs rate. Its dominant feature is **altitude** (importance 0.67),
  followed by inclination (0.12) and element age (0.11) — the index's own
  two components rank second and third, which is a partial vindication of
  the hand-crafted design.
- **Element freshness varies by object class**: median public element age
  is 9-18 hours depending on group; science satellites are tracked best,
  and roughly a quarter of cubesats carry elements older than 24 hours.
- **Low altitude dominates unreliability**: the ISS scores only "reduced"
  reliability not from poor data but because ~418 km altitude is the most
  drag-sensitive regime — the same physics that forces frequent station
  reboosts.
- **Reliability varies by population**: station-group objects score worse
  (median ORI 53.5) than cubesats (63.9), debris (66.3) and science
  satellites (68.6) purely through orbital regime.

### Known gaps

- **The space-weather term is untested.** Every hour collected so far is
  quiet, so the ORI environment factor has been a constant 1.0 in practice.
  The factor now *can* vary (Gold reads the full accumulated history rather
  than a 24-hour rolling file), but validating it requires a disturbed
  period.
- **SatNOGS outcomes are still accumulating.** The collector now sweeps
  terminal statuses and re-checks passes whose window has closed, so
  outcomes will arrive; until one does, the good-pass rate is reported as
  unknown rather than as zero.


## Repository Structure

```text
src\      ingestion, loading, quality gate, orchestration, analysis, API
sql\      SQL transformations that build the Gold tables
tests\    pytest suite (no network access; in-memory DuckDB fixtures)
scripts\  Windows scheduled-task setup and removal
monitoring\  Prometheus scrape config and Grafana provisioning
docs\lessons.md   the 24-lesson build log, kept as a lab notebook
.github\workflows\   lint, tests, and import checks
docker-compose.yml   MinIO + Redpanda + Prometheus + Grafana
data\     bronze/ silver/ gold/ reports/ quality/ logs/  (not committed)
```

## Development

```powershell
pip install -r requirements.txt -r requirements-dev.txt
python -m pytest          # 165 tests, no network access
python -m ruff check src tests
```

Tests cover the parts that are easy to get silently wrong: SGP4 unit
conversions and the RIC decomposition, the rank-correlation statistics,
the quality-gate operators, orchestrator locking and timeout handling, the
collectors' pure logic, and each Gold SQL transformation run against
in-memory fixtures.

## Configuration

Local service credentials come from the environment. Copy the template and
edit it before exposing any port beyond localhost:

```powershell
Copy-Item .env.example .env
```

The compose file publishes every port on `127.0.0.1` only, and the API has
no authentication: it is read-only over public data, but `/metrics` exposes
the full research state, so keep it local.

## Design Principles

- **Bronze is immutable evidence.** Every snapshot is checksummed; the loader
  refuses to re-load or accept modified files, and the quality gate reports
  any Bronze file that no loader pattern claims.
- **Gold reads history, not the latest file.** Source feeds are rolling
  windows; only the accumulated history describes the whole collection
  period.
- **Point-in-time correctness.** Element age is measured from each element's
  own epoch to its snapshot time, never against "now".
- **Honest science.** Unknown is reported as unknown, never as zero.
  Measurements outside a model's validity envelope are labelled and
  excluded from statistics rather than silently averaged in or deleted.
- **Provider respect.** Per-source rate limits enforced in the orchestrator,
  independent of the hourly schedule, and a bounded request count per run.
- **Idempotency everywhere.** Every step is safe to re-run; the pipeline
  takes an atomic lock because DuckDB allows one writer.

## Licence

MIT for the code. The collected data belongs to CelesTrak, NOAA SWPC, and
the SatNOGS Network under their own terms — see [LICENSE](LICENSE).

